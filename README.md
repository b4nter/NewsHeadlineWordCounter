# NewsHeadlineWordCounter

Application to count word frequency in news titles. 
It scans RSS Feeds and stores titles in the file. Then from that file counts word frequency.

Work still in progress...
